<header class="header">
    <a href="/" class="logo">Магазин на ООП</a>
    <i class="bx bx-menu" id="menu-icon"></i>
    <nav class="navbar">
        <a class="active" href="/">Главная</a>
        <a href="/catalog">Каталог</a>
        <a href="/basket">Корзина(<span id="count">{{ count }}</span>)</a>
    </nav>
    <div class="auth">
        {% if isAuth %}

            {{ userName }}
            <a href="/auth/logout">[X]</a>

        {% else %}

            <form class="login" action="/auth/login" method="post">
                <div class="input-box">
                   <div> <input type="text" name="login" placeholder="Логин"></div>
                   <div> <input type="password" name="pass" placeholder="Пароль"></div>
                  <div>  <input class="btn" type="submit" name="submit" value="Войти"></div>
                </div>
            </form>

        {% endif %}
    </div>
</header>
